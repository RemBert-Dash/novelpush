# Working memory

- The approved NovelPush board uses Ink Blue `#0B1D33`, Gold `#D4AF37`, Ivory `#F5F2E9`, Slate `#51607A`, and Mist `#E3E4E6` with an editorial dark-tech world.
- Use Lora for display/headlines and Inter for interface/body text.
- Pricing must say checkout is coming soon until real platform product IDs exist; never add Stripe.
- Email-only capture must use `/site/notify` with `subscriber.added` and `surfaceToCeo: false`.
- Starter requires the footer badge "Built with MadeThis" linking to `https://madethis.com/r/bgdw5cgx`.
- On August 25, 2026, the validated rebuild source was repackaged as `novelpush-source.zip`: a flat, Windows-compatible ZIP with `package.json` and source folders at its root, excluding `node_modules/` and `.next/`. CRC and clean-extraction checks passed; see `.agent/packaging-report.md`.
